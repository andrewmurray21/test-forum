<% if @forums.any? %>
  <% @forums.each do |f| %>
    <forum_main class="col-md-8">
      <section class="forum_info"
        <h3><%= f.title %></h3>
        <h5>Topics: <%= forum_topics_count(f) %>, 
        Posts: <%= forum_posts_count(f) %></h5>
      </section>
    </forum_main>
    <forum_main class="col-md-4">
      <section class="last_post_info"
        <% if forum_empty?(f) %>
           <h3>No posts in this forum</h3>
        <% else %>
           <h3>Latest: <%= latest(f).topic.title %></h3>
           <h5><%= latest(f).user.name %>, 
           <%= time_ago_in_words(latest(f).created_at) %> ago</h5>
        <% end %>
      </section>
    </forum_main>
  <% end %>
<% end %>





        
